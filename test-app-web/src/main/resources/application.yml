spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/test_app?serverTimezone=UTC
    username: root
    password:
  jpa:
    show-sql: true
    database: MYSQL
    hibernate:
      ddl-auto: update
  thymeleaf:
    cache: false

